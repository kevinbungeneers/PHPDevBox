---
- name: Installing required packages
  apt:
    pkg: "{{ item }}"
    state: present
    update_cache: yes
  with_items:
    - python-mysqldb
    - mysql-server-5.6
    - mysql-client-5.6

- name: Making sure MySQL is running
  service:
    name: mysql
    state: started